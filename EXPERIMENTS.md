> Lean index of experiments — one row per experiment, kept cheap to read. This is the entry point for "what have we already done?": consult it before designing or re-running an experiment, then Read only the relevant `md/experiments/` file. Full analyses live in `md/experiments/`; raw artifacts in `results/`; cross-experiment synthesis in RESEARCH.md.
> Below the table lives the experiments map — a `mermaid` DAG of experiments with the motivating findings on the edges (see [`DIAGRAMS.md`](DIAGRAMS.md)).
> (See [`EXPERIMENT_ANALYSIS.md`](EXPERIMENT_ANALYSIS.md) for the full convention.)

# Experiments index

| id | date | question | one-line finding | file |
|----|------|----------|------------------|------|
| ...  | ...  | ...      | ...              | ...  |

## Experiments map

*(`mermaid` DAG of the experiments above — an edge means "motivated/unlocked", labelled with the high-level, research-relevant finding that led to the child experiment (no low-level details); drawn per [`DIAGRAMS.md`](DIAGRAMS.md).)*
